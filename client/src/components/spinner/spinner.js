import React from "react";
import "./spinner.css";

// create a variable Spinner, assign a function returning the element to it, and export it
export const Spinner = () => {
    // return spinner element
    return <div className="spinner" />;
}